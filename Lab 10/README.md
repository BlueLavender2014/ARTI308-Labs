In this assignment, we used Support Vector Machines (SVM) to classify the Iris dataset. First, we imported the required libraries and explored the dataset using pairplots and KDE plots to visualize the different flower species. We observed that the setosa species was the most separable. Then, we split the data into training and testing sets and trained an SVM model using Scikit-learn. After making predictions, we evaluated the model using a confusion matrix and classification report, where the model achieved perfect accuracy. Finally, we applied GridSearchCV to tune the hyperparameters of the SVM model, but the results remained the same because the original model already performed perfectly.

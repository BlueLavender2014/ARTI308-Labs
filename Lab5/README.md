In this lab, I built a model to predict order status using the Talabat set. I applied feature engineering and trained a Random Forest model.

I tested a new feature, different peak-hour definitions, and different values for item categories, but the accuracy stayed almost the same. I also applied feature selection, which simplified the model without improving performance.

All in all, the model performed consistently, and the changes had little impact on accuracy.
